server {
    listen 80;
    server_name _;

    # Frontend React (build estático)
    location / {
        root  /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # financas-api
    location /api/ {
        proxy_pass         http://financas-api:8001/api/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   Authorization $http_authorization;
        proxy_read_timeout 60s;
        client_max_body_size 20m;
    }

    # ia-api
    location /ia/ {
        proxy_pass         http://ia-api:8002/ia/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   Authorization $http_authorization;
        proxy_read_timeout 120s;
        client_max_body_size 20m;
    }

    # ─── Para produção com HTTPS (Let's Encrypt) ─────────────────────────────
    # Descomente o bloco abaixo e comente este server block, seguindo o README.
    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }
}

# ─── Bloco HTTPS (produção com domínio e certificado) ────────────────────────
# Descomente quando tiver domínio + cert Let's Encrypt configurado no VPS.
#
# server {
#     listen 443 ssl;
#     server_name SEU_DOMINIO.com;
#
#     ssl_certificate     /etc/letsencrypt/live/SEU_DOMINIO.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/SEU_DOMINIO.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location / {
#         root  /usr/share/nginx/html;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://financas-api:8001/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_read_timeout 60s;
#         client_max_body_size 20m;
#     }
#
#     location /ia/ {
#         proxy_pass http://ia-api:8002/ia/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_read_timeout 120s;
#         client_max_body_size 20m;
#     }
# }
